spring.application.name=book-manager
spring.datasource.url=jdbc:mysql://localhost:3306/book_manager
spring.datasource.username=root
spring.datasource.password=1234
server.port=10222
lombok.equalsAndHashCode.callSuper = call

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

#docker commands
#docker run -p 6033:3306 --name=docker-mysql -e MYSQL_ROOT_PASSWORD=1234 -e DATABASE_NAME=book_manager -d mysql:latest
#docker run -t --link docker-mysql:mysql -p 10222:10222 book_manager
##error
##https://github.com/moby/moby/issues/25562